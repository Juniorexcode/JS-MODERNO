Math.floor(2.4)

const num1 = 10;
const num2 = 20;

//PI
let result = Math.PI;


// Redondear
result = Math.round(2.8);
result = Math.round(2.2);
result = Math.round(2.5);
result = Math.round(2.1);
result = Math.round(2.9);


// Redondear hacia arriba
result = Math.ceil(2.5); // 3

// Redondear hacia abajo
result = Math.floor(2.9); // 2

// Raiz cuadrada
result = Math.sqrt(144);

// Valor absoluto
result = Math.abs(-4);

// Potencia
result = Math.pow(2, 4); // 16

// Numero Minimo entre un conjunto de numeros
result = Math.min(-4, 3, 5, 100, 4);

// Numero Maximo entre un conjunto de numeros
result = Math.max(-4, 3, 5, 100, 4);

// Numeros Aleatorios
result = Math.random();

// Numeros Aleatorios entre un rango
result = Math.floor(Math.random() * 10);


console.log(result);